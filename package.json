{
  "name": "tryproperpdf-site",
  "private": true,
  "scripts": {
    "build": "python3 generate.py",
    "generate": "python3 generate.py",
    "dev": "npm run build && npm run db:migrate:local && wrangler dev --port 8788",
    "deploy": "npm run build && wrangler deploy",
    "check:deploy": "npm run build && test -f public/index.html && test -f public/sitemap.xml && test -f public/robots.txt && test -f public/_headers && test -f public/_redirects && npm run check:worker",
    "test": "node --test tests/*.test.cjs tests/*.test.mjs",
    "db:migrate:local": "wrangler d1 migrations apply properpdf-site-data --local",
    "db:migrate:remote": "wrangler d1 migrations apply properpdf-site-data --remote",
    "check:functions": "npm run check:worker",
    "check:worker": "wrangler deploy --dry-run --outdir=.wrangler/worker"
  },
  "devDependencies": {
    "wrangler": "^4.137.0"
  },
  "type": "module"
}
